package com.pheonix.orchestrationengine.repository;

import org.springframework.data.jpa.repository.JpaRepository;

public interface MemoryInfoRepository extends JpaRepository<MemoryInfoEntity, Long> {
}
